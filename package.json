{
    "name": "@coveops/variant-facet",
    "version": "1.0.0",
    "description": "Variant Facet, with all nested queries",
    "main": "dist/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "coveops build variant-facet --template typescript",
        "watch": "coveops build variant-facet --template typescript --watch",
        "serve": "coveops serve",
        "precompile": "npm run build"
    },
    "keywords": [],
    "author": "",
    "license": "Apache-2.0",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "dependencies": {
        "@coveops/turbo-core": "^1.1.1",
        "coveo-search-ui": "^2.9659.2"
    },
    "devDependencies": {
        "@coveops/cli": "^0.11.0"
    }
}
